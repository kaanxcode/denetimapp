import { StyleSheet, Text, View } from "react-native";
import React from "react";

const firebaseSend = ({ question, answer, noteText }) => {
  return (
    <View>
      <Text>firebaseSend</Text>
    </View>
  );
};

export default firebaseSend;

const styles = StyleSheet.create({});
